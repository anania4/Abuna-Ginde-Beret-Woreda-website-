{
  "name": "woredanew",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "npm run build && cp -r dist/* backend/public/ && cd backend && npm run start",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "vite",
    "build:frontend": "vite build",
    "deploy:frontend": "npm run build && cp -r dist/* backend/public/"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.24",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.18",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@tailwindcss/postcss": "^4.1.18"
  }
}
